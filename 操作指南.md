# Hexo 博客操作指南

## 新建导航菜单

### 1. 修改主题配置

文件：`themes/landscape/_config.yml`

```yaml
menu:
  Home: /
  Archives: /archives
  关于: /about
  项目: /projects
```

### 2. 创建对应页面

#### 关于页面
文件：`source/about/index.md`

```markdown
---
title: 关于
date: 2026-02-22
type: about
---

## 关于我

你好，我是Charm1ng，目前是一名计算机研究生。

### 技术栈

- 嵌入式开发
- 深度学习
- Web开发

### 联系方式

- Email: example@email.com
- GitHub: github.com/yourname
```

### 3. 重新构建

```bash
hexo clean
hexo generate
```

## 项目展示功能

### 原理

通过自定义Generator实现类似Archives的projects索引页。

### 实现步骤

#### 1. 安装依赖

```bash
pnpm add hexo-pagination
```

#### 2. 创建自定义Generator

文件：`scripts/projects-generator.js`

```javascript
'use strict';

const pagination = require('hexo-pagination');

hexo.extend.generator.register('projects', function(locals) {
  const { config } = this;
  
  const projectsDir = 'projects';
  const paginationDir = config.pagination_dir || 'page';
  const perPage = config.per_page || 10;
  const result = [];

  const projectPosts = locals.posts.filter(post => post.type === 'projects');
  
  if (!projectPosts.length) return result;

  function generate(path, posts, options = {}) {
    options.projects = true;
    
    result.push(...pagination(path, posts, {
      perPage,
      layout: ['archive', 'index'],
      format: paginationDir + '/%d/',
      data: options
    }));
  }

  generate(projectsDir, projectPosts);

  return result;
});
```

#### 3. 创建项目文章

在 `source/_posts/` 目录下创建markdown文件，Front-matter添加 `type: projects`

```markdown
---
title: 项目名称
date: 2026-01-15
type: projects
categories: 项目
tags: [标签1, 标签2]
---

## 项目简介

项目描述...
```

#### 4. 主题menu已配置

主题配置中已有 `项目: /projects`，无需修改。

### 效果

- 访问 `/projects/` 显示项目列表
- 类似Archives归档页效果

## 常用命令

```bash
hexo clean          # 删除构建缓存
hexo generate       # 构建静态文件
hexo server         # 运行本地服务器
```
